import React from 'react';
import useTitle from '../../hooks/useTitle';

const AddAToy = () => {
   useTitle('Add a toy')

   return (
      <div>
         
      </div>
   );
};

export default AddAToy;